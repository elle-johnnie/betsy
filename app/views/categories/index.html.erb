<main class="main-container">
  <h1>Categories</h1>

  <ul>
  <% @categories.each do |cat| %>
    <li><%= cat.category.capitalize %></li>
  <% end %>
  </ul>

  <%= link_to "Add a new category", new_category_path, class: "btn btn-small" %>
</main>
